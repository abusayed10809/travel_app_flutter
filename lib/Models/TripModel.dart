class TripModel{
  final String title;
  final DateTime startDate;
  final DateTime endDate;
  final double budget;
  final String travelType;

  TripModel(this.title, this.startDate, this.endDate, this.budget, this.travelType);
}